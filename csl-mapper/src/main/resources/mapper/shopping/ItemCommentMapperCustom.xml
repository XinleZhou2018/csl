<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.csl.mapper.shopping.ItemCommentMapperCustom">
<!--    COUNT(NULL)=0, 如果只写COUNT(comment_level = 1)，当comment_level!=1的时候，表达式为false，COUNT(false)=1，也会被计数-->
    <select id="queryItemCommentLevel" parameterType="String" resultType="com.csl.pojo.vo.shopping.ItemCommentLevelVO">
        SELECT
	        COUNT(*) AS totalCount,
	        COUNT(comment_level = 1 OR NULL) AS goodCount,
	        COUNT(comment_level = 2 OR NULL) AS meduimCount,
	        COUNT(comment_level = 3 OR NULL) AS badCount
        FROM
	        items_comments
        WHERE
	        item_id = #{itemId};
    </select>

	<select id="queryItemComments" parameterType="Map" resultType="com.csl.pojo.vo.shopping.ItemCommentVO">
		SELECT
		ic.comment_level as commentLevel,
		ic.content as content,
		ic.sepc_name as specName,
		ic.created_time as createdTime,
		u.face as userFace,
		u.nickname as nickname
		FROM
		items_comments ic
		LEFT JOIN
		users u
		ON
		ic.user_id = u.id
		WHERE
		ic.item_id = #{paramsMap.itemId}
		<if test=" paramsMap.level != null and paramsMap.level != '' ">
			AND ic.comment_level = #{paramsMap.level}
		</if>
	</select>
</mapper>