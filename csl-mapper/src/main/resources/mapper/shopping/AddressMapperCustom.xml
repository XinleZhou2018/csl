<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.csl.mapper.shopping.AddressMapperCustom">
    <select id="AddressMapperCustom" parameterType="String" resultType="com.csl.pojo.shopping.UserAddress">
        SELECT
            id,
            user_id AS userId,
            receiver,
            mobile,
            province,
            city,
            district,
            detail,
            extand,
            is_default AS isDefault,
            created_time AS createdTime,
            updated_time AS updatedTime
        FROM
            user_address
        WHERE
            user_id = #{userId}
    </select>

    <insert id="addUserNewAddress" parameterType="com.csl.pojo.shopping.UserAddress">
        INSERT INTO
            user_address(id, user_id, receiver, mobile, province, city, district, detail, extand, is_default, created_time, updated_time)
        VALUES (
           "1", "a1",  #{userAddress.receiver}, #{userAddress.mobile}, #{userAddress.province}, #{userAddress.city}, #{userAddress.district}, #{userAddress.detail}, #{userAddress.extand}, #{userAddress.isDefault}, NOW(), NOW()
        )
    </insert>
</mapper>